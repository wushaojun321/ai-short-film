# Production overrides: remove bind-mount volumes so containers use image code
services:
  api:
    volumes: []

  worker-llm:
    volumes: []

  worker-image:
    volumes: []

  worker-video:
    volumes: []

  worker-merge:
    volumes: []
